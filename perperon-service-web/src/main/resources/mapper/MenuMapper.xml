<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.perperon.mall.mapper.MenuMapper">
<resultMap id="BaseResultMap" type="com.perperon.mall.pojo.Menu">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="menu_name" jdbcType="VARCHAR" property="menuName" />
    <result column="perms" jdbcType="VARCHAR" property="perms" />
    <result column="component" jdbcType="VARCHAR" property="component" />
    <result column="path" jdbcType="VARCHAR" property="path" />
    <result column="visible" jdbcType="VARCHAR" property="visible" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="created" jdbcType="TIMESTAMP" property="created" />
    <result column="status" jdbcType="BIT" property="status" />
    <result column="sort" jdbcType="INTEGER" property="sort" />
    <result column="level" jdbcType="INTEGER" property="level" />
</resultMap>
    <sql id="basic_list">
        a.id,a.menu_name, a.perms, a.component, a.path, a.visible, a.icon, a.user_id, a.created, a.status, a.sort, a.level
    </sql>
    <select id="listByPage" parameterType="map" resultMap="BaseResultMap">
        select <include refid="basic_list"/>
        from pp_menu as a
        <where>
            <if test="menuName != null and menuName != ''">
               and a.menu_name like concat('%', #{menuName}, '%')
            </if>
        </where>
    </select>
    <select id="selectPermsByUserId" resultType="string" parameterType="string">
        select
            a.perms
        from
            pp_account_role as ur
            left join pp_roles as r on
            ur.role_id = r.id
            left join pp_role_menu as rm on
            r.id = rm.role_id
            left join pp_menu as a on
            rm.menu_id = a.id
        where
            ur.account_id = #{userId}
          and a.status = 1
          and r.status = 1
    </select>

    <select id="getMenuCodeByRoleId" resultMap="BaseResultMap" parameterType="string">
        select <include refid="basic_list"/>
        from pp_menu as a
        left join pp_role_menu rm on rm.menu_id = a.id
        <where>
            <if test="roleId != null and roleId != ''">
                and rm.role_id =#{roleId}
            </if>
        </where>
    </select>
</mapper>